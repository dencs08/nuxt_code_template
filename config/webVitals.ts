// config/webVitals.ts

export default {
    provider: "log", // log, ga, gtm, api
    // ga: { id: '' }, // Google Analytics ID
    debug: true, // debug enable metrics reporting on dev environments
    disabled: false
};
